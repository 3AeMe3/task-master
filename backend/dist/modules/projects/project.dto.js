export function toProjectDto(project) {
    return {
        id: project.id,
        name: project.name,
        userId: project.userId,
        description: null,
    };
}
//# sourceMappingURL=project.dto.js.map